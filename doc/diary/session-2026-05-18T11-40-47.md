# Cursor session export — `draw_faces`, indexed `Cube::visible_faces`, filled-cube milestone ship

**Timestamp:** `2026-05-18T11-40-47` (shell `date`)

## Context

Continuation on **Thorus Forge** — CPU software rasterizer in Rust (**macOS**). Authoritative planning: [`doc/planning/project-spec.md`](../planning/project-spec.md), [`doc/planning/project-breakdown.md`](../planning/project-breakdown.md).

## Work completed this session

### 1. `draw_faces()` and facet palette (`src/lib.rs`)

- Added **`pub const CUBE_FACE_PALETTE: [Rgb; 6]`** — one flat color per **`Cube::faces`** slot after **`Cube::default`**, aligned with normals **−Z, +Z, +X, −X, +Y, −Y** (order documented beside the const).
- Added **`pub fn draw_faces`** — same view direction / BF culling story as **`draw_edges`**; projects quad corners via **`Camera::transform`**, fills with **`FillQuad`**, tint from **`CUBE_FACE_PALETTE[face_idx]`**.

### 2. Indexed visible face stream (`src/scene/cube.rs`)

- **`Cube::visible_faces`** generalized to **`impl Iterator<Item = (usize, Quad)>`**: stable **`faces`** slot **`0…5`** through **`Cube::transform`**, paired with Corners unfolded from **`CubeFace::verts`** after the same **`!is_back`** filter used for edges.
- Eliminated **`visible_faces_indexed`** naming in favor of a single **`visible_faces`** API; callers that only need geometry use **`.map(|(_, q)| q)`** (see struct rustdoc).

### 3. Commits (chronological)

1. **`62af52f`** — `feat: draw_faces with facet palette and indexed visible_faces` — library + cube API (**2 files**: `lib.rs`, `scene/cube.rs`).
2. User asked for another commit after milestone wiring (see below). **`bba9a90`** — `feat(exports): ship filled faceted cube in still/animated WebPs` — exporters, golden **`snapshots/cube/scene.webp`**, integration test regenerate hint, planning doc (**5 files**).

### 4. Milestone — “Cube: quad stream — filled faceted raster” (shipped)

**Plan (executed):**

| Piece | Detail |
|--------|--------|
| Still export | [`src/bin/still-cube.rs`](../../src/bin/still-cube.rs) — **`draw_faces`** replaces **`draw_edges`**; **`//!` docs** describe filled faceted cube |
| Animated export | [`src/bin/animated-cube.rs`](../../src/bin/animated-cube.rs) — **`draw_faces`** per frame after **`clear_black`**; **`WIRE_RGB`** dropped |
| Golden regression | Regenerated **`snapshots/cube/scene.webp`** with **`cargo run --quiet --bin still-cube -- snapshots/cube/scene.webp`** |
| Test doc | [`tests/cube_snapshot.rs`](../../tests/cube_snapshot.rs) regenerate comment lists **`--bin still-cube`** explicitly |
| Planning | [`doc/planning/project-breakdown.md`](../planning/project-breakdown.md) — subsection marked **`[x]`**, **`Shipped`** bullet added, wireframe BF milestone **Outcome** clarified as historical “at this milestone”; **Notes / deferred** iteration bullet updated (**shipped** quad fill wording) |

**Verification:** **`cargo fmt`**, **`cargo clippy --all-targets`**, **`cargo test`** all green after changes.

### 5. Library surface (reference)

- **`thorus_forge::draw_faces`**, **`CUBE_FACE_PALETTE`**, existing **`draw_edges`**, **`FillQuad`**, **`Cube::visible_faces`**, **`visible_edges`**.
- **Depth buffer** intentionally still deferred — single convex exterior cube acceptable without **`z`** for this interim quad path (**per breakdown**).

## Follow-ups / not done here

- Optional **edge overlay** from surviving quads + **`draw_line`** (debug parity with old wireframe) — milestone optional only.
- Introducing a nominal **`Vertex`** / **`[Vertex; 4]`** stream — spec allows slot + **`Quad`** at raster boundary until **sphere → triangle unification**.
- **`doc/diary/session-2026-05-18T11-26-19.md`** — older diary stub may still exist alongside this export; consolidate or delete if desired.

## Technical notes

- **`FillQuad`** uses bbox scan + consistent winding half-plane tests (`src/framebuffer/fill_quad.rs`).
- **`Camera::transform`** ignores **`z`** for **xy** mapping (ortho phase); matches absence of depth test for this milestone.
