# Session diary — `2026-05-10T09:23` (local)

Export of the Cursor session for **rust-3d-rasterizer**: continuing the WebP-first export path (`doc/planning/project-spec.md`, `project-breakdown.md`). Focus: **`FrameBuffer`**, **`Rgb`**, **`set_pixel`**, milestone closure for **Base WebP (still)**.

---

## 1. Plan — extracting `FrameBuffer` from `main`

- Discussed extracting framebuffer concerns (dimensions + RGB slab) into a dedicated module so `main` stays I/O–oriented (`output_file_name`, encoder, prints).
- **Tests first:** new `FrameBuffer` must allocate **`width × height × 3`** bytes and zero-fill (`vec![0u8; …]`).

---

## 2. `FrameBuffer` module (`src/framebuffer.rs`)

- Added **`pub struct FrameBuffer`** holding internal **`rgb: Vec<u8>`**.
- **`new(width, height)`** allocates **`pixel_count × 3`** zeros.
- **`impl AsRef<[u8]> for FrameBuffer`** for **`WebpEncoder::add_frame`**, which already takes **`impl AsRef<[u8]>`**.
- **`#[cfg_attr(not(test), allow(dead_code))]`** was used briefly until **`main`** referenced the type (**`not(test)`** ⇒ allow unused in normal builds); removed once **`main`** constructed a **`FrameBuffer`**.

---

## 3. Commits (mid-session)

- **`86f7bf4`** — *feat: extract FrameBuffer from main* — **`mod framebuffer`**, **`Encoder` uses framebuffer instead of raw `Vec`**.

---

## 4. Narrowing API and tests

- Dropped getters **`width()` / `height()`**; **`main`** keeps **`SCENE_WIDTH` / `SCENE_HEIGHT`** for the encoder alongside **`FrameBuffer::new(..)`**.
- Unit test **`initialize_frame_buffer`** on **10×10**: length **`10×10×3`**, all bytes zero.

---

## 5. `Rgb` + `set_pixel`

- **`pub struct Rgb(pub u8, pub u8, pub u8)`** (tuple newtype for **`R,G,B`**), **`derive(Copy, …)`**.
- **`FrameBuffer`** gained **`width` / `height`** fields again **only** so **`set_pixel`** can bounds-check.
- **`set_pixel(&mut self, x, y, color)`** — row-major **`(y × width + x) × 3`**; **`x >= width` or `y >= height`** ⇒ **silent return** (skip-only, aligns with raster spec wording).
- **`c5441df`** — *feat: add set_pixel, Rgb, and blue center WebP still* — **`main`** drew **`Rgb(0, 0, 255)`** at **`(width/2, height/2)`**; **`//!`** adjusted.

Tests:

- **`set_pixel_writes_rgb_at_correct_offset`** — **3×3** grid, **`set_pixel(1, 1, PIXEL_COLOR)`**, compare full buffer bytes to an inlined expected **`[u8; 27]`** using **`PIXEL_COLOR.{0,1,2}`**; **`#[rustfmt::skip]`** on the **`let expected = […]`** binding to keep readable row layout.
- **`set_pixel_out_of_bounds_is_ignored`** — **`(100, 0)`** on **`2×2`** leaves buffer zero.
- Removed **`set_pixel_out_of_bounds_does_not_erase_existing_pixels`** — redundant once OOB is a strict no-write early return.

Test-only **`const PIXEL_COLOR: Rgb = Rgb(10, 20, 30)`**.

---

## 6. Rust note — **`#[cfg_attr(not(test), allow(dead_code))]`**

- Explained separately: **`cfg_attr(condition, attr)`** attaches **`allow(dead_code)`** **only when not building tests**, so **`cargo build`** stayed quiet before **`main`** wired **`FrameBuffer`**; **`cargo test`** still used **`FrameBuffer`**.

---

## 7. Alignment with **`doc/planning`**

- **Base WebP (still):** exporter should produce valid lossless **`800×600`** still from **RGB** slab; outcome text originally **blue** center pixel (sanity stride / corner / encoder).
- Implemented center highlight, then switched to **`Rgb(255, 255, 255)`** (white).
- **`doc/planning/project-breakdown.md`**: outcome line updated to **white** pixel; heading **`### [x] Base WebP (still)`** when milestone treated as shipped.

---

## 8. Git

- **`git log --oneline -20`** shared on request (`86f7bf4`, `c5441df`, RGB switch, WEBP wiring, …).
- **Verification:** **`cargo fmt --check`**, **`cargo clippy -D warnings`**, **`cargo build`**, **`cargo test`** — all green (unit framebuffer tests + **`tests/binary_writes_valid_webp.rs`** decoding output WebP).

**Final workspace commit:**

- **`4bcf2aa`** — *docs: complete Base WebP still milestone* — staging **`project-breakdown.md`**, **`src/main.rs`** (white center + **`//!`**), **`src/webp_encoder.rs`** ( **`Error`** no longer in import list — **`webp_animation::Error`** in **`Result`** signatures). Single-line message per request to omit long detail bullets.

---

## 9. Open threads / next boxed milestone

- **`doc/planning/project-breakdown.md`**, next unchecked: **Drawing lines** (DDA stepping → crossed-square still **`800×600`**).
- **Golden / pixel-diff tests** explicitly deferred per spec; integration test continues to assert **decoder accepts** artifact only.
