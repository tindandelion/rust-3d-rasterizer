# Session diary — `2026-05-14T10:16:05Z`

## Goals (this session)

1. Summarize accumulated orthographic-projection milestone work after the user deleted **Case 17** (look-at tests) for later.
2. Persist that summary into a new repo-root Markdown file **`orth_proj_summary.md`**.

Secondary (this message): **`/diary-entry`** skill — archive the session under **`doc/diary/`**.

## Context inherited from earlier work

- **TDD style:** **`tests/ortho_projection.rs`** defines behavior; **`src/projection.rs`** implements helpers until **`cargo test --test ortho_projection`** passes. User prefers **tests from the assistant** and **implementation on their side**.
- **Convention shift:** Raster mapping uses the **whole framebuffer only** (`NDC xy` spans corner pixel centers with `(W−1)`, `(H−1)` scales). **`ndc_to_viewport_px`**, **`ortho_point_to_viewport_pixel`**, **`orth_world_to_viewport_pixel`** and related cases (**6**, **12**, **15**) were intentionally removed earlier; **`projection.rs`** and the test suite comment both state fullscreen-only framing.
- **Case 17 (look-at):** User chose to **drop** associated tests **for now**. **`projection.rs`** still contains stub APIs **`view_matrix_look_at_lh`** / **`orth_world_to_clip_look_at_lh`** (not exercised by `# ortho_projection` after removal). Intended direction when restored: match **`glam::Mat4::look_at_lh`**, **`orth_world_to_clip`** = **`Proj * View`**; align **Rust** signatures (**`up`** vector, etc.) with rustdoc expectations.

## What we did today

### `orth_proj_summary.md` added (repo root)

New high-level recap document documenting:

- **Coordinate / raster conventions:** LH **`+Y` up**, **`+Z` forward** (aligned with **`doc/planning/project-spec.md`**); framebuffer top-left origin, **`+y` down** on the bitmap; NDC **`+y` up** implies Y flip in **`ndc_to_framebuffer_px`**; column-vector **`glam`** multiply order.
- **Active test cases (by number):** **1, 2, 3, 4, 5, 7, 8, 9, 10, 11, 13, 14, 16** with a compact table tying each to the **primarily exercised** **`projection`** API (`orth_projection`, `orth_projection_matrix`, pixel pipeline helpers, **`u16`** depth codec, **`orth_world_*`** with translation-only view).
- **Removed numbering:** viewport cases (**6**, **12**, **15**) and **look-at Case 17** called out explicitly.
- **`projection.rs`** API grouped by logical pipeline stages; **look-at stubs** flagged as unfinished / untested and notes about **`Proj * View * p`** symmetry with Case **13**.

During authoring, corrected a typo in draft copy (**"+Y forward"** → **"+Z forward"**) and tightened the **look-at** paragraph about signature/rustdoc **`up`** alignment.

### Verification

Ran:

```text
cargo test --test ortho_projection
```

**Result:** **13** integration tests compiled and passed (no Case 17 harness present).

### Files touched (this trajectory)

| Path | Role |
|------|------|
| `orth_proj_summary.md` | Session deliverable — milestone narrative + case table |
| `doc/diary/session-2026-05-14T10-16-05.md` | This diary entry |

(Existing edited sources such as **`tests/ortho_projection.rs`**, **`src/projection.rs`**, **`Cargo.*`** reflected **prior** user/agent work; unchanged in the summary-only turn except as already on branch.)

## Open threads / suggested next moves

- Reintroduce **Case 17** strictly as **`tests`** (user implements **`view_matrix_look_at_lh`**, **`orth_world_to_clip_look_at_lh`**, optionally export from **`lib`** as needed).
- Consider **`world + look_at → UVec2`** mirroring **`orth_world_to_framebuffer_pixel`** once clip path is nailed.
- **Model matrix:** extend to **`Proj * View * Model`** when mesh vertices live in model space.

## Narrative takeaway

Orthographic derivation is formally **paused on look-at** while the rest of the pipeline (ortho **`[vmin,vmax] → [-1,1]³`**, NDC **`xy`** on full framebuffer, clamp/quantize, linear **`z`** + **`u16`**, translation-only **`world→clip/framebuffer`**) is locked in tests and summarized for quick onboarding or future checkpoints.
